# 系统公告

## AI 模型网关面板（路由器原生版）

把多家大模型的免费额度聚合到一起，一个接口调用；对话不断联，稳定性媲美官方接口。

- **路由器原生运行**：装进 iStoreOS / OpenWrt 软路由，7×24 小时常驻，不用开 Windows 电脑
- **多平台聚合**：支持 NVIDIA、商汤 SenseNova、魔搭 ModelScope 等，免费额度可叠加使用
- **自动故障切换 + 熔断**：某个渠道限流 / 故障 / 超时，自动换下一个，对话不中断
- **路由组一键配置**：内置 `256k`（长文本 / 代码库）、`1m`（超大上下文）两个路由组，自动挑最优模型
- **实时监控面板**：每个模型的健康状态、可用率、延迟一目了然
- **可选外部存储**：配置可存到外接硬盘，重装 / 升级不丢配置

> 建议升级到最新版；路由器端在「已安装」里点更新即可，配置不丢失。

---

## 新手提示

1. 没配过？进面板点「⚡ 一键配置」，照着填各家 API Key 就行
2. 没注册过平台？每个平台注册都有免费额度，全部加起来可长期使用
3. 配好后，在你的 AI 客户端里填 `256k` 或 `1m` 作为模型名，网关自动选最好的模型

---

## 完整使用手册

> 以下为完整使用教程全文（适用于 iStoreOS / OpenWrt 软路由原生版）。

### 一、安装与启动

1. 在 iStoreOS 的「iStore 应用商店」里搜索 **AI 模型网关面板**（包名 `luci-app-model-gateway`），点安装。
   - 或者用 `opkg install luci-app-model-gateway`（需先配置好软件源）。
2. 安装后在「已安装」里把 **AI 模型网关面板** 启用（开机自启打勾）。
3. 启用后服务监听 `12211` 端口，进程名 `model-gatewayd`。

> 确认运行：路由器里 `netstat -lntp | grep 12211` 能看到监听；或面板能打开即正常。

### 二、打开管理面板

- 方式一：iStoreOS 应用页点「AI 模型网关面板」直接打开。
- 方式二：浏览器访问 `http://<你的路由器LAN IP>:12211`
  （例如路由器管理地址是 `192.168.1.1`，那就访问 `http://192.168.1.1:12211`）

打开后就是「AI 模型网关面板」，主要区域：

- 顶部「⚡ 一键配置」「📢 系统公告」按钮
- 左侧/顶部标签：**实时监控**、**消耗统计**
- 「本地接口信息」卡片：显示你的网关地址和 API Key（AI 客户端要用）

### 三、注册上游接口（拿各家 Key）

AI 模型网关面板本身不提供 Key，需要你去各家官网免费注册。每家约 3–5 分钟，全搞定不到半小时。

#### 3.1 NVIDIA

| 项目 | 内容 |
|---|---|
| 官网 | https://build.nvidia.com |
| 注册方式 | 邮箱注册；获取 Key 时可能要手机验证 |
| Base URL | `https://integrate.api.nvidia.com/v1` |
| 额度 | 每天多次调用，够日常使用 |

步骤：打开官网 → 右上角 Sign In 注册 → 进 Explore 选模型（如 DeepSeek-V4-Flash）→ 点「Get API Key」→ 复制 `nvapi-xxxx`。

#### 3.2 商汤 SenseNova

| 项目 | 内容 |
|---|---|
| 官网 | https://platform.sensenova.cn/console/keys |
| 注册方式 | 手机号 / 邮箱注册 |
| Base URL | `https://token.sensenova.cn/v1` |
| 额度 | 注册即送额度 |

步骤：打开链接 → 注册登录 → 控制台 → API 管理 → 复制 `sk-xxxx`。

#### 3.3 魔搭 ModelScope

| 项目 | 内容 |
|---|---|
| 官网 | https://modelscope.cn |
| 注册方式 | 手机号 / 邮箱；需绑定阿里云账号才能用 API |
| Base URL | `https://api-inference.modelscope.cn/v1` |
| 额度 | 每天不固定 Token 额度 |

步骤：打开官网 → 注册登录 → 账号设置 → 访问控制 → 创建并复制 `ms-xxxx` → **务必绑定阿里云并完成认证**。

### 四、一键配置（小白最省事）

1. 在面板里点「⚡ 一键配置」。
2. 把上面拿到的 Key 分别填进对应平台（NVIDIA / 商汤 / 魔搭）。
   - 已经添加过的平台可以**留空**，保留原配置；填了新 Key 会覆盖旧的。
3. 点「一键应用」。
4. 等几秒，回到主页的「实时监控」，会看到各模型状态：
   - 🟢 绿色 = 正常
   - 🔴 红色 = 异常
   - ⚪ 灰色 = 还没检测到

> 系统默认每 5 分钟自动巡检一次所有渠道，不用手动管。

### 五、把网关接到你的 AI 客户端

#### 5.1 拿到网关连接信息

在面板「本地接口信息」卡片里能看到：

```
本地地址：  http://<路由器LAN IP>:12211/v1
API Key：   sk-local-xxxxxxxxxxxx
```

把这两项填进你的 AI 客户端（任何兼容 OpenAI 格式的工具都行，比如 ChatBox、OpenWebUI、WorkBuddy 等）。

#### 5.2 用路由组，不用记模型名

一键配置默认生成两个路由组：**`256k`**（长文本 / 代码库）和 **`1m`**（超大上下文）。

在 AI 客户端的「模型」一栏，直接填路由组名而不是具体模型名：

```
模型：  256k
```

网关收到请求后会：
1. 在当前路由组里筛出所有正常的模型；
2. 按可用率 + 延迟算质量分，挑最好的；
3. 逐个尝试，遇到 429 / 5xx / 超时自动换下一个（熔断 60 秒后重试）。

> 你完全无感，看到的只是一个正常回复。日常**优先用路由组名**。

#### 5.3 指定具体模型（进阶）

想固定走某个模型，模型名填 `供应商-模型名` 格式，例如：

```
模型：  NVIDIA-deepseek-ai/deepseek-v4-flash
```

> 指定具体模型不会自动切换，该渠道挂了请求就失败；日常还是推荐路由组。

### 六、认识监控面板

- **实时监控**：每个模型前的状态灯、SLA 可用率（24h 正常比例）、平均 / 最快 / 最慢延迟、探针次数；右上角可按可用率排序、按供应商筛选、搜关键词、点「立即检测」手动巡检。
- **消耗统计**：按今日 / 7 天 / 30 天统计总请求数、输入 / 输出 / 合计 Token，并按下「供应商 × 模型」展示明细。
- **上游提供商**：查看 / 修改 / 禁用 / 删除各家 API 配置，重新拉模型列表。
- **路由配置**：查看和新建路由组、调整组内模型。

### 七、存储路径（可选）

默认配置存在路由器内部 `/etc/config/model-gateway`。如果你的路由器存储空间小、或想重装不丢配置，可以在面板把「配置文件路径」切到外接硬盘（如 `/mnt/mmc1-4/Configs/model-gateway`）。

> 切路径**点一次「保存并应用」即可生效**（当前版本已修复需点两次的问题）。

### 八、常见问题

**Q：面板打不开？**
A：确认服务已启用；浏览器访问 `http://<路由器LAN IP>:12211`。路由器重启后若没自启，去「已安装」确认开机自启已勾选。

**Q：模型状态全红？**
A：依次检查 ① Key 是否复制完整 ② Base URL 是否带 `/v1` 结尾 ③ 路由器能否访问外网 ④ 点「立即检测」重测。

**Q：能加更多供应商吗？**
A：能。只要对方提供 OpenAI 兼容接口，在「上游提供商」里添加即可。

**Q：路由组全挂了会怎样？**
A：组内全不可用时降级尝试红色（异常）模型；仍不行返回 503，点「立即检测」或等下次自动轮询恢复。

**Q：本地接口信息里的地址是 127.0.0.1 还是路由器 IP？**
A：面板会自动显示你实际访问用的地址。从其他设备连，请用路由器 LAN IP（如 `192.168.1.1:12211`），不是 `127.0.0.1`。

### 九、一句话总结

> 配置一次，长期可用。不用再管 Key，不用再担心断联。
> 各家 Key 配好后，日常只要：打开 AI 客户端 → 输入问题 → 拿回答。背后是 NVIDIA 还是商汤在响应，你不用关心。

---

项目地址：https://github.com/wanvfx/luci-app-model-gateway/
