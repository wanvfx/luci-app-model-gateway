# 📢 系统公告 · AI 模型网关面板（路由器原生版）

> 🌟 **当前版本：v1.5.3**（即 r20260728c 批次）。建议升级到最新版；路由器端在「已安装」里点更新即可，配置不丢失。

---

## 🤖 它是什么

把多家大模型的免费额度聚合到一起，一个接口调用；对话不断联，稳定性媲美官方接口。

- 🧩 **路由器原生运行**：装进 iStoreOS / OpenWrt 软路由，7×24 小时常驻，不用开 Windows 电脑
- 🔗 **多平台聚合**：支持 NVIDIA、商汤 SenseNova、魔搭 ModelScope 等，免费额度可叠加使用
- 🔄 **自动故障切换 + 熔断**：某个渠道限流 / 故障 / 超时，自动换下一个，对话不中断
- 🧭 **路由组一键配置**：内置 `256k`（长文本 / 代码库）、`1m`（超大上下文）两个路由组，自动挑最优模型
- 📡 **实时监控面板**：每个模型的健康状态、可用率、延迟一目了然
- 💾 **可选外部存储**：配置可存到外接硬盘，重装 / 升级不丢配置

---

## 📌 版本更新说明

### 🌟 上一版 r20260726z 已实现（保留，基础能力）

- ✅ 多提供商聚合（任意 OpenAI 兼容 `base_url`）+ 模型统一前缀名
- ✅ 路由组（默认 `256k` / `1m`）+ 5 种策略：质量 / 优先级 / 最低延迟 / 最低成本 / 轮询
- ✅ 自动故障转移 + 熔断 / 健康探活（面板状态灯 🟢🔴⚪）
- ✅ 识图（多模态）：带图请求转视觉模型理解
- ✅ 一键配置预设（NVIDIA / 商汤 / 魔搭 等）
- ✅ 实时监控 / 调用日志 / 外部存储 / 系统公告 / 检查更新 / 深浅主题

### 🚀 最新版 v1.5.3（即 r20260727c 批次）功能与增强

> 一句话：在稳定基础上补齐「多租户管控、省钱可视、智能派单、隐私护栏」四大块，并对照 Python 参考版（1.6.1）修正探测与免费模型判定，消灭"未分类"。

- 🔑 **虚拟密钥（子密钥）+ 每密钥配额限流**：给不同人/客户端发独立钥匙，可限每日请求数 / Token / 模型白名单，支持启停与吊销（数据落 `MODEL_GATEWAY_DATA/vkeys.json`）
- 🛡️ **严格能力矩阵**：开关开启后，按模型能力过滤候选（如带图请求只投视觉模型），杜绝"文不对题"
- 💰 **成本 / 用量仪表盘**：按渠道 / 模型 / 日统计估算费用（USD），含预算卡与趋势表（顶部「📊 统计」标签页下半部分）
- 🔀 **内容分类路由**：`classify` 策略 + `cat=组` 成员格式，按请求内容（vision/code/long/reasoning/general）纯本地规则自动分流
- 💱 **价格自动同步**：后台轮询上游价格覆盖参考库，成本估算更准（可 `POST /api/price-sync` 手动触发）
- 🕵️ **PII 正则脱敏**：响应自动打码手机号 / 身份证 / 邮箱 / 银行卡
- 🧪 **A/B 条件路由**：按条件将流量分到不同模型组
- 🎯 **动态惩罚路由**：连续失败的候选自动降权，更快避开不稳定模型
- 📊 **Prometheus `/metrics`**：暴露请求数 / 延迟 / Token / 错误率，可接 Grafana 监控大盘
- 🔁 **幂等键**：`Idempotency-Key` 防止客户端重试重复计费
- 🔐 **API Key AES 加密落盘**：密钥写入 UCI 时自动加密（主密钥 `MODEL_GATEWAY_DATA/vault.key`），内存仅用明文
- 📊 **消耗统计与成本统计合并到同一「📊 统计」标签页**，一页看全

### 🐞 v1.5.1 → v1.5.3 稳定性与对齐修复

- 🔧 **v1.5.1 健康检测对齐**：改用轻量端点探活，对齐预设 Key 校验，消除 SLA 偏低 / 延迟虚高 / 误报全红。
- 🛠️ **v1.5.2 全量审计修复 9 项**：流式中途故障转移乱码、虚拟密钥白名单越权、缓存并发竞争、模型名整段替换污染 prompt、熔断失败计数窗口衰减、分类路由递归护栏、配置重载漏解密导致全红、全失败兜底提示、init.d 自定义 stop 覆盖导致服务停不掉。
- ✨ **v1.5.3 对照 Python 1.6.1 修复 4 项**：
  - ① 免费模型检测接入 pricing 判定免费，NVIDIA / 商汤 / 魔搭等免费模型正确启用；
  - ② `Catalog.LookupOrDefault` 兜底，消灭"未分类"模型；
  - ③ 探测回退 `ChatProbe` 逐模型真实 `POST /chat/completions` + 别名解析，成功/失败状态对齐 Python；
  - ④ 保存配置不再触发即时探测，仅 300s 周期 + 每日 12 点复查，探测频率对齐 Python。
- 🖼️ **图标补全**：iStoreOS 商店现已显示应用图标（不再破图）。
- 🛠️ **最新批次 r20260728c 增量修复（在 r20260727c 基础上）**：
  - 🔋 **智能巡检计数封顶**：修正每日 12 点静默复查仍累加「密集巡检进度」的 bug，徽标永久封顶 `20/20`（旧版会逐日 +1，约 19 天涨到 39，与"约 20 次后休眠"矛盾）；「立即检测」按钮不受影响。
  - 🎛️ **模型卡片启用/停用按钮（新功能）**：稳定性页每行新增「操作」列——启用=绿色「● 启用」、停用=灰色「○ 停用」，默认启用；停用的模型行整体置灰，点击即时写入 UCI `disabled_models` 并热生效，无需重启服务。
  - 🐞 **修复高危前端 bug**：删除了导致整段 SPA 脚本解析失败的多余 `}`，此前会让所有按钮 onclick 全部失效，现已恢复正常。
  - 🧩 **成本与用量统计页布局修复**：修正标题行容器漏闭合导致预算卡 / 汇总卡 / 三张表格横向错乱的问题，恢复正常纵向布局。

---

## 📘 完整使用教程（v1.5.3）

> 以下为完整使用教程全文（适用于 iStoreOS / OpenWrt 软路由原生版）。

## 智能聚合路由网关 · 完整使用教程

> 适用版本：`luci-app-model-gateway` v1.5.3（即 r20260728c 批次；iStoreOS / OpenWrt 原生应用，端口 12211）
> 面向用户：第一次用、想"照着做就能跑"的小白。所有步骤均对应面板里真实存在的按钮与弹窗。
> 本版（v1.5.3）在 v1.5.0 四大块（多租户管控 / 省钱可视 / 智能派单 / 隐私护栏）基础上，叠加 v1.5.2 全量审计修复 9 项、v1.5.3 对照 Python 1.6.1 的 4 项对齐修复（免费模型检测 / 消灭未分类 / 探测机制 / 探测频率），并补全了商店图标。完整功能地图见第八节。

---

### 一、它是什么（一句话 + 白话）

这个网关是一个装在路由器里的"AI 万能插座"：

- 你手里有好几个大模型账号（比如 OpenAI、DeepSeek、硅基流动、本地 Ollama……），每个账号的接口都不一样、模型名也不一样。
- 装了它之后，**所有客户端（ChatGPT 客户端、OpenClaw、ComfyUI、你自己的脚本）只认一个地址、一个密钥**，由它帮你把请求转发到最合适的那个账号和模型上。
- 它还能帮你：一个模型挂了自动换另一个（故障转移）、便宜的模型优先用（省钱）、相同问题直接复用答案（缓存）、限制并发别打爆上游、超预算自动提醒、把长名字的内部模型改个好记的别名、给不同人发不同的"子密钥"并分别限流配额。

> 类比：它就像公司前台的"总机"——外面只拨一个总机号，前台按规则帮你转给对应的人；人不在就转下一个；常问的问题前台直接答；现在前台还能给每个部门发不同的分机号并限制通话时长。

---

### 二、核心概念（先看这 4 个，后面不迷路）

| 概念 | 白话解释 | 面板里对应 |
|---|---|---|
| **提供商（Provider）** | 你的一個 AI 账号（一个 base_url + 一个 api_key），下面挂着若干模型 | 「实时监控」页的卡片，+ 添加提供商 |
| **路由组（Router）** | 把几个模型捆成"一组"，客户端只点组名，网关按策略挑一个回 | 🔀 路由配置 → + 添加路由 |
| **模型前缀名** | 网关对外暴露的模型 id 格式是 `提供商名-模型名`（如 `deepseek-deepseek-chat`） | 客户端请求时填这个名 |
| **虚拟密钥（子密钥）** | 给某个"人/客户端"单独发的一把钥匙，可限每日请求数/Tokens、限可用模型 | 🔑 虚拟密钥 |

**客户端怎么连它**（记住这两样，后面都要用）：
- 基地址（Base URL）：面板「实时监控」页顶部显示的 `http://路由器IP:12211/v1`
- API Key：可以是管理员的 `sk-local-xxxx`（面板顶部「本地访问密钥」），也可以是你下发的**虚拟密钥** `sk-vk-xxxx`（🔑 虚拟密钥里创建）

---

### 三、首次使用标准流程（先做什么 → 后做什么 → 达成什么）

> 目标：装好后，让任意一个 OpenAI 兼容客户端能通过一个地址访问你所有的模型，并且高可用、能省钱、能分人管控。

#### 步骤 1：登录面板，拿到连接信息
1. 浏览器打开 `http://<路由器IP>:12211`。
2. 首次会要求输入 `admin_key`（首次启动自动生成，也在路由器 `/etc/config/model-gateway` 里）。
3. 登录后停在「实时监控」页。
4. **复制两样东西备用**：页面顶部的「本地访问地址」和「本地访问密钥」（点右边的「复制」按钮）。
   - ✅ 达成：你已经知道客户端要填的 Base URL 和默认 API Key。

#### 步骤 2：添加你的第一个提供商
1. 点顶部 **「+ 添加提供商」**。
2. 填写：
   - 名称（自定义，如 `deepseek`）
   - Base URL（如 `https://api.deepseek.com`）
   - API Key（你的账号密钥，落盘时自动 AES 加密，明文仅内存使用）
   - 模型列表（可留空，点「测试连接」后自动拉取）
3. 点 **「测试连接」** 验证能通；通了点 **「保存并拉取模型」**。
4. 弹出的「模型管理」里勾选你**想对外暴露**的模型 → **「保存更改」**。
   - ✅ 达成：面板「实时监控」出现一张提供商卡片，点「⚙️ 模型」能看到它下面的模型。

#### 步骤 3：把模型暴露给客户端（可选但推荐）
- 点顶部 **「📦 管理输出模型」** 可查看/调整当前对外暴露的全部模型名（前缀名）。
- 客户端填模型名时就用这里的 `提供商-模型` 格式。
   - ✅ 达成：你知道客户端该填什么模型名。

#### 步骤 4：配置路由（实现"高可用 + 省钱 + 智能分类"）
1. 点顶部 **「🔀 路由配置」**。
2. 点 **「+ 添加路由」**，新建一个路由组：
   - 组名（如 `my-ai`）—— 这就是客户端要填的"模型名"。
   - 成员：从下拉里勾选多个模型的**前缀名**（如 `deepseek-deepseek-chat`、`silicon-gpt-4o`）。
   - **策略**（决定挑哪个）：
     - `quality` 质量优先（默认，按健康分挑最好的，并叠加动态惩罚）
     - `priority` 严格按你拖拽的顺序（手动定优先级）
     - `least-latency` 延迟最低优先
     - `cost` 最便宜优先（省钱）
     - `loadbalance` 轮流分摊（均衡）
     - `classify` **内容分类**（按请求内容自动分流，见下方说明）
   - 拖拽成员行可调整 `priority` 顺序。
3. 点 **「保存所有路由」**。
   - ✅ 达成：客户端只要填 `my-ai` 这个模型名，网关就会在组内自动挑模型；某个挂了自动换下一个。
4. **（可选）内容分类路由**：若策略选 `classify`，成员要写成 `分类=子组` 的形式：
   - 在「成员」里逐个添加，格式为 `cat=组名`，例如：
     - `vision=视觉组`（处理带图请求）
     - `code=代码组`（处理代码问题）
     - `long=长文组`（处理超长文本）
     - `default=默认组`（兜底，接不住的分类都走它）
   - 每个 `cat=组名` 对应一个**普通路由组名**；网关会对请求正文做纯规则启发式分类（vision / code / long / reasoning / general），命中就转给对应子组，未命中走 `default`。
   - ✅ 达成：代码问题自动走代码组、图片问题自动走视觉组，互不影响健康度与成本。

#### 步骤 5：给模型起别名（可选）
- 在「路由配置」弹窗下方「别名映射」区，点 **「+ 添加别名」**：
  - 别名（如 `gpt4`）→ 真实模型（如 `openai-gpt-4o`）。
- 点 **「保存别名」**。
   - ✅ 达成：客户端可以用 `gpt4` 这种短名代替一长串前缀名。

#### 步骤 6：打开"省钱 + 防打爆 + 能力保护"的防护（网关设置）
1. 点顶部 **「⚙️ 网关设置」**。
2. **响应缓存**：打开开关 → 设 TTL（如 3600 秒）、最大条数。
   - 作用：相同问题直接复用答案，省调用、提速（流式也会"回放"给用户，体验无缝）。
3. **预算护栏**：填「每日上限（美元）」、动作选 `warn`（到线提醒）或 `block`（到线拦截）、预警比例（如 80%）。
   - 作用：花钱到预警线，面板顶部弹橙色提醒；到上限可自动拦新请求。
4. **并发护栏**：填「全局最大并发」（如 20）。
   - 作用：同时请求太多时排队/返回 429，避免打爆上游账号被封。
5. **🛡️ 严格能力矩阵**：打开开关。
   - 作用：开启后，网关会**严格按模型能力过滤候选**——例如请求带图片时，绝不会把"纯文本模型"选进去；需要视觉能力的请求只投给带 `vision` 能力的模型。配合「识图配置」使用更稳。
6. **REST 钩子**（可选）：点「+ 添加钩子」，填你的回调 URL，选触发事件（`request_done` / `request_failed`）。
   - 作用：每次请求完成/失败自动通知你的系统（可接飞书/企微机器人做告警）。
7. 点 **「保存全部设置」**。
   - ✅ 达成：缓存、预算、并发、能力矩阵、钩子全部生效，面板顶部预算 banner 每 30 秒自动刷新。
> 价格参考库会**后台自动同步**上游价格（落到 `models_catalog_sync.json`，覆盖参考价）；如需手动立即触发，可对网关发 `POST /api/price-sync`（管理员鉴权），或等后台自动轮询。

#### 步骤 6（续）：发放虚拟密钥（子密钥 + 配额限流）
> 用途：你不想把管理员 `admin_key` 发给别人，而是给每个"人 / 客户端"发一把独立的钥匙，并可分别限制用量。

1. 点顶部 **「🔑 虚拟密钥」** 打开管理弹窗。
2. 点 **「+ 新建密钥」**，填写：
   - 名称（方便你识别，如 `给小明`）
   - 每日请求配额（0 = 不限制；超过返回 429）
   - 每日 Token 配额（0 = 不限制）
   - 允许模型（留空 = 允许全部；填则只放行指定前缀名）
   - 备注（可选）
3. 点 **「创建」**——**关键**：弹窗会**只显示一次**明文密钥（形如 `sk-vk-xxxxxxxx`），立刻点「复制」保存好（之后看不到完整明文，列表里是脱敏的 `sk-vk-****`）。
4. 列表里可看到每把密钥的当日已用请求/Token、启停状态；点开关可**启用/停用**，点「删除」可吊销。
   - ✅ 达成：你把 `sk-vk-xxxx` 发给对方，对方当普通 API Key 用（`Authorization: Bearer sk-vk-xxxx`）；超额/停用/到期会自动 429，不影响你的 `admin_key` 和其他密钥。
> 虚拟密钥数据落 `MODEL_GATEWAY_DATA/vkeys.json`（随外部存储或 `/var/lib/model-gateway`），不在 UCI 里，重装/升级不丢。

#### 步骤 7：在客户端里填好，开用
- 任意 OpenAI 兼容客户端：Base URL 填步骤 1 复制的地址，API Key 填 `sk-local-xxxx`（管理员）**或** `sk-vk-xxxx`（虚拟密钥），模型名填 `my-ai`（路由组名）或某个 `提供商-模型` 前缀名或别名。
   - ✅ 达成：客户端通过统一入口访问全部模型，自动故障转移、缓存、预算护栏、配额限流全部生效。

---

### 四、分场景操作示例（适用情况 / 操作步骤 / 预期结果）

#### 场景 1：我只有一个账号，就想统一管理模型名
- **适用情况**：单提供商（如只有 DeepSeek），但嫌模型名太长、客户端只认 OpenAI 格式。
- **步骤**：步骤 2 添加提供商 → 步骤 3 确认输出模型 → 客户端直接填 `deepseek-deepseek-chat`。
- **预期结果**：客户端用 OpenAI 格式直连成功，无需路由组。

#### 场景 2：多账号做"高可用"，一个挂了自动换
- **适用情况**：有 DeepSeek + 硅基流动两个账号，怕某个临时抽风。
- **步骤**：分别添加两个提供商并拉模型 → 路由配置建组 `ha` → 成员勾两个账号的同类型模型 → 策略选 `quality`（默认，按健康分挑最好，并叠加动态惩罚）→ 保存。
- **预期结果**：`ha` 组里某个模型 5xx/超时，网关自动换组内下一个；面板「实时监控」里该模型会标红（熔断中），恢复后自动回来。

#### 场景 3：省钱优先（便宜模型先上）
- **适用情况**：日常问答为主，想尽量用便宜/免费模型。
- **步骤**：路由组策略选 `cost` → 成员里把免费/便宜模型排在前面（cost 会按参考库价格自动升序）→ 网关设置开「预算护栏」设每日上限。
- **预期结果**：请求优先走便宜模型；当日花费接近上限时顶部弹橙色预警，超线可拦截。

#### 场景 4：给内部模型起好记的别名
- **适用情况**：内部模型名很长（如 `provider-xxx-anthro-claude-3-5-sonnet-20241022`），客户端不想记。
- **步骤**：路由配置 → 别名映射 → `claude` → `provider-xxx-anthro-claude-3-5-sonnet-20241022` → 保存别名。
- **预期结果**：客户端直接填 `claude` 即可命中真实模型。

#### 场景 5：用缓存降低延迟与费用（含流式）
- **适用情况**：大量重复相似问题（如 FAQ、批量摘要）。
- **步骤**：网关设置 → 响应缓存开 → TTL 3600、最大条数 1000 → 保存。
- **预期结果**：首次回答正常走上游并缓存；相同/近似问题二次命中缓存，**非流式直接返回、流式以打字机方式无缝回放**，延迟与调用量明显下降；「网关设置」里可看命中率、可「清空缓存」。

#### 场景 6：防止打爆上游 + 失败自动通知
- **适用情况**：多个客户端共用，怕瞬间并发把账号限流。
- **步骤**：网关设置 → 并发护栏全局最大并发设 20 → REST 钩子加一个你的企微/飞书机器人 Webhook URL，事件勾 `request_failed` → 保存。
- **预期结果**：并发超 20 时新请求排队/返 429；任一上游请求失败，你的机器人收到失败通知（钩子带 SSRF 防护，仅允许 http(s) 且内网地址受限）。

#### 场景 7：让网关"看懂图"（识图）
- **适用情况**：想用支持多模态的模型处理图片。
- **步骤**：点顶部 **「🖼️ 识图配置」** → 开启识图 → 选择用于看图的描述模型（如带视觉能力的模型）→ 保存。
- **预期结果**：客户端发带图片的请求时，网关把图片转给视觉模型理解，再按原有路由返回文字结果。配合「🛡️ 严格能力矩阵」开启，可进一步保证带图请求只投给有视觉能力的模型。

#### 场景 8：事后复盘花了多少、哪个模型最忙
- **适用情况**：月底想看成本与用量。
- **步骤 A（调用/Token 视角）**：切到顶部 **「📊 统计」** 标签页 → 上半部分「消耗统计」选「近 24 小时 / 近 7 天 / 近 30 天」→ 看调用量、Token、各模型稳定性。
- **步骤 B（成本/美元视角，本版新增）**：同一 **「📊 统计」** 标签页的下半部分「成本与用量统计」→ 选天数（7/14/30）→ 看：
  - 顶部汇总卡：总请求、总 Token、估算总费用（USD）
  - 预算卡：当日/周期花费 vs 预算上限，超线标红
  - 按渠道表：每个 provider 的花费与占比
  - 按模型表：每个模型的花费与占比
  - 按日表：每天花费趋势
- **预期结果**：图形化看到哪天花钱多、哪个模型最贵，指导下一轮路由/预算策略调整。成本按参考库价格 × Token 估算（价格会自动同步，无需手填）。

#### 场景 9：给不同人发不同的钥匙并分别限流
- **适用情况**：把网关共享给朋友/同事，但不想给管理员密钥，还要限制每人每天用量。
- **步骤**：🔑 虚拟密钥 → + 新建密钥（给小明，每日请求 500、Token 200000、允许模型留空=全部）→ 复制一次性明文 `sk-vk-xxxx` 发给对方。
- **预期结果**：小明用 `sk-vk-xxxx` 正常访问；当天用超配额自动 429；你在列表里能看到他的当日用量，可随时停用/删除。

#### 场景 10：按内容自动分流（代码走代码组、图片走视觉组）
- **适用情况**：你接了多种专用模型，希望"代码问题自动去代码组、图片问题自动去视觉组"，而不是全挤在一个组里。
- **步骤**：路由配置 → 新建路由 `smart` → 策略选 `classify` → 成员依次加 `vision=视觉组`、`code=代码组`、`long=长文组`、`default=默认组`（这些名字是你别的普通路由组名）→ 保存。
- **预期结果**：客户端只需填 `smart`；网关对请求正文做规则分类后自动委托到对应子组；分类规则是纯本地启发式（识别代码块/图片标记/超长文本/推理关键词），不上传任何内容。

#### 场景 11：严格按能力派单，避免"文不对题"
- **适用情况**：你有一堆模型，有些只支持文本、有些支持视觉；怕带图请求误投到纯文本模型导致报错。
- **步骤**：⚙️ 网关设置 → 打开「🛡️ 严格能力矩阵」→ 保存。
  - **预期结果**：路由候选被严格按所需能力过滤；带图请求只投给带 `vision` 能力的模型，其余请求不误用专用模型，稳定性提升。

#### 场景 12：临时停用某个模型（无需停服务）
- **适用情况**：某个模型限流 / 抽风 / 想下线，但不想删配置、也不想重启服务。
- **步骤**：切到顶部 **「📊 统计」** 标签页 → 上半部分「消耗统计」找到对应模型的行 → 行尾「操作」列点 **「○ 停用」**（灰色按钮）→ 按钮变 **「● 启用」**（绿色）、该行整体置灰。
- **预期结果**：该模型立即被排除出路由候选（写入 UCI `disabled_models` 并热生效），新请求不再选它；想恢复时点「● 启用」即可。状态一眼可见，不用进「⚙️ 模型」去翻。

---

### 五、进阶技巧

- **一键配置（预设）**：点 **「⚡ 一键配置」** 选官方/社区预设方案，点「一键应用」快速铺好提供商+路由（适合不想手填的小白）。
- **系统公告**：点 **「📢 系统公告」** 可看作者发布的使用提示/版本说明（数据来自 `announcement.md`）。
- **检查更新**：点 **「🔄 检查更新」**，有新版本会弹窗，点「立即更新」即可（版本号三处同步，更新后浏览器强制刷新）。
- **主题切换**：点右上角 🌓 在深/浅色间切换。
- **检测全部**：点「检测全部」一键探活所有提供商与模型健康状态（对应后端巡检）。
- **Prometheus 指标（运维向）**：网关在 `/metrics` 暴露请求数、延迟、Token、错误率等指标，可用 Prometheus + Grafana 拉取做监控大盘（不影响普通使用）。
- **幂等键（开发向）**：对同一请求带上 `Idempotency-Key` 请求头，短时间重复提交只计费/执行一次，防止客户端重试导致重复扣费。

---

### 六、客户端接入速查

| 你要填的项 | 填什么 | 哪里拿 |
|---|---|---|
| Base URL | `http://<路由器IP>:12211/v1` | 面板「实时监控」顶部「本地访问地址」 |
| API Key | 管理员 `sk-local-xxxx` **或** 虚拟密钥 `sk-vk-xxxx` | 管理员：`sk-local-xxxx` 在面板顶部「本地访问密钥」；虚拟密钥：🔑 虚拟密钥里创建（仅显示一次） |
| 模型名 | 路由组名（如 `my-ai`/`smart`）或 `提供商-模型`（如 `deepseek-deepseek-chat`）或别名（如 `claude`） | 路由配置 / 管理输出模型 / 别名映射 |

> 管理员 `admin_key` 与虚拟密钥都通过 `Authorization: Bearer <key>` 鉴权；虚拟密钥可单独限流/停用/吊销，适合分发给他人。

---

### 七、常见问题（排错）

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 客户端报 401 | API Key 不对 / 没带 `Bearer ` | 复制面板密钥或虚拟密钥，请求头写 `Authorization: Bearer sk-xxxx` |
| 报 404 model not found | 模型名写错 | 用「管理输出模型」里的 `提供商-模型` 前缀名，或路由组名/别名 |
| 某个模型一直红（熔断） | 该上游连续失败 | 等健康探活恢复；或检查该提供商 api_key / base_url；可临时在「模型管理」禁用它 |
| 花钱太快 | 没开预算护栏 | 网关设置 → 预算护栏设每日上限 + 动作 `block` |
| 上游频繁 429 | 并发太高 | 网关设置 → 并发护栏调小全局最大并发 |
| 虚拟密钥请求直接 429 | 该密钥当日配额用尽 / 已停用 / 已到期 | 🔑 虚拟密钥列表查看当日用量与开关；调大配额或重新启用 |
| 虚拟密钥创建后丢了明文 | 明文仅显示一次 | 列表里是脱敏的；只能删除旧密钥再新建一把 |
| 带图请求总失败 | 路由投到了纯文本模型 | 开「🛡️ 严格能力矩阵」；或路由组用 `classify` 把图片分流到视觉组 |
| 输出里敏感信息被替换成 `[REDACTED]` | 已启用 PII 正则脱敏 | 属正常隐私保护；如确需关闭请在网关设置/配置中调整该能力开关 |
| 面板打不开 | 服务没起 / 端口被占 | 路由器里确认 `model-gateway` 服务已启用并运行（`/etc/init.d/model-gateway status`） |

---

### 八、功能地图（当前版本已具备）

**路由与高可用**
- ✅ 多提供商聚合（任意 OpenAI 兼容 base_url）
- ✅ 6 种路由策略：质量（含动态惩罚）/ 优先级 / 最低延迟 / 最低成本 / 轮询 / **内容分类 `classify`** + `auto` 虚拟模型
- ✅ 故障转移（全候选顺序切换）+ 熔断 / 健康探活
- ✅ **🛡️ 严格能力矩阵**（按模型能力过滤候选，避免文不对题）
- ✅ **A/B 条件路由**（按条件分流体）
- ✅ 别名映射
- ✅ 识图（多模态）

**省钱与护栏**
- ✅ 语义缓存（精确 + 近重复 simhash，非流式直返 / 流式 SSE 回放）
- ✅ 预算/余额护栏（按日 warn/block）+ 实时预警 banner
- ✅ 并发护栏（全局 + 单提供商两级）
- ✅ **价格自动同步**（后台轮询覆盖参考价，手动可触发）
- ✅ **PII 正则脱敏**（响应自动打码，保护隐私）
- ✅ **🔑 虚拟密钥（子密钥）+ 每密钥配额限流**（请求数 / Token / 模型白名单 / 启停 / 吊销）

**可观测与运维**
- ✅ 统一管理面板（实时监控 / 📊 统计（消耗 + 成本合并）/ 路由配置 / 网关设置 / 调用日志 / 模型参考库）
- ✅ **🔑 虚拟密钥管理弹窗**（列表/创建/启停/删除，用量按自然日清零）
- ✅ **模型卡片启用/停用**（稳定性页「操作」列，即时热生效；停用的模型行置灰，一眼可见状态）
- ✅ **Prometheus `/metrics` 指标**
- ✅ **幂等键**（防重复计费）
- ✅ **API Key AES 加密落盘**（vault 主密钥 `MODEL_GATEWAY_DATA/vault.key`）
- ✅ REST 钩子（SSRF 防护 + 可选 HMAC）
- ✅ 一键配置预设、系统公告、检查更新、深浅主题

> 仍在规划（暂未做，见《功能差异与集成可行性评估》）：内置对话测试台（Playground）、HTTP MCP 工具调用等。（注：v1.5.0 的多租户/成本/分类/隐私能力、v1.5.2 全量审计 9 项、v1.5.3 对齐 Python 的 4 项修复与图标补全均已落地，详见上方"版本更新说明"。）

---

## 🔗 项目地址

https://github.com/wanvfx/luci-app-model-gateway/
